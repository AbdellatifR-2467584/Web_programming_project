<!DOCTYPE html>
<html lang="nl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Volg mee: <%= post.title %>
    </title>
    <meta name="description" content="Volg je recept mee.">
    <link rel="icon" type="image/png" href="/resources/logo/Logo_cirkel.png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/followAlong.css">
    <script src="/js/followAlong.js" defer></script>
</head>

<body>
    <h1 id="postTitel" data-id="<%= post.id %>">
        <%= post.title %>
    </h1>

    <!-- Ingrediëntenlijst -->
    <section class="ingredienten" data-ingredients='<%- JSON.stringify(post.ingredients) %>'>
        <h2>Ingrediënten</h2>
        <p style="text-align: center; margin-bottom: 20px;">Vink alles aan wat je klaar hebt staan.</p>

        <ul class="list-ingredienten"></ul>

        <button id="selectAllBtn" class="secondary-btn">Alles selecteren</button>
        <button id="startCookingBtn" class="action-btn">Start met koken</button>
    </section>

    <!-- Stappen (verborgen totdat de gebruiker de ingrediënten heeft aangevinkt) -->
    <section class="instructies" data-steps='<%- JSON.stringify(post.steps) %>' style="display:none">

        <label class="tts-toggle">
            <input type="checkbox" id="ttsToggle" checked>
            Auto-play Audio
        </label>

        <h2>Stap <span id="stepCount">1</span></h2>

        <div class="progress-container">
            <div class="progress-bar" id="progressBar"></div>
        </div>

        <button id="replayBtn" class="replay-btn" aria-label="Speel audio opnieuw af">
            <i class="bi bi-volume-up-fill"></i>
        </button>

        <p id="currentStep"></p>

        <div class="controls">
            <button id="prevStep" class="control-btn secondary" disabled>
                <i class="bi bi-arrow-left"></i> Vorige
            </button>
            <button id="nextStep" class="control-btn">
                Volgende <i class="bi bi-arrow-right"></i>
            </button>
        </div>
    </section>
</body>

</html>